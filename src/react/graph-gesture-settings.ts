/** Publicly unexported constants shared by the recognisers and their tests. */
export const GRAPH_GESTURE_SETTINGS = Object.freeze({
  tapSlop: 8,
  tapMaxDuration: 500,
  longPressDuration: 500,
  longPressPointerCount: 1,
})
